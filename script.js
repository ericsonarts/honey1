// script.js

document.addEventListener('DOMContentLoaded', () => {
  const startBtn = document.getElementById('start-btn');
  startBtn.addEventListener('click', () => {
    // Placeholder for navigation or functionality
    alert('Welcome to Honey Helper!');
  });
});
